files:
  /etc/profile.d/mongo.sh:
    content: |
          mongostart() {
            sudo mongod --fork --logpath /var/log/mongodb.log --dbpath=/var/data
          }

          mongostop() {
            sudo mongod --dbpath=/var/data --shutdown
          }